<!--
このファイルは機能追加候補を GitHub Issue としてそのまま貼れる形でまとめています。
共通ラベル案: feature / enhancement / ux / performance / architecture
-->

# 機能追加イシュー集 (コピー用テンプレ)

優先度レベル(提案段階):
- P0: 早期投入でコア価値を高める
- P1: 次期スプリント候補
- P2: 中期的拡張

---

## :one: カンバンビュー (/kanban) の実装でタスク進捗を視覚化する
**優先度:** P0  
**カテゴリ:** feature / ux / productivity

### 背景 / 現状
`TodoApp` から `/kanban` へのリンクが存在するがページ未実装。`status` プロパティ (todo / in-progress / done) が型定義とデータに既に存在しており、UI 追加の土台はある。

### 目的 (Value)
- 状態遷移をドラッグ&ドロップで直感的に操作
- チーム / 個人の進捗把握向上

### スコープ (In Scope)
- `/kanban` ページ追加 (App Router)
- カラム: Todo / In Progress / Done
- カード: タイトル + Priority バッジ + 作成日時 (相対表示)
- DnD (例: `@dnd-kit/core` または `react-beautiful-dnd`)
- 状態更新時 `status` と `completed` の同期

### 非スコープ (Out of Scope)
- Swimlane (担当者別) 表示
- 外部同期 / リアルタイムコラボ

### 提案 (Solution)
1. `src/app/kanban/page.tsx` を追加
2. `useTodos` (将来分離予定) からステートを再利用 (または暫定で `TodoApp` とロジック共有ユーティリティ)
3. ドラッグ完了時に `updateStatus()` 呼び出し
4. モバイル: カラム縦積み / スクロール

### タスクリスト
- [ ] 依存ライブラリ選定 & インストール
- [ ] Kanban ページ実装
- [ ] DnD 状態遷移ロジック
- [ ] カードコンポーネント共通化 (既存 TodoItem の再利用検討)
- [ ] テスト: 状態移動 / 永続化反映

### 受け入れ基準
- [ ] カードをドラッグで status を変更できる
- [ ] `done` カラムへ移動すると `completed=true`
- [ ] リロード後もカンバン表示が保持
- [ ] モバイル幅でレイアウト崩れなし

---

## :two: 期限 (Due Date) と期限ベースのソート・ハイライト機能
**優先度:** P1  
**カテゴリ:** feature / time-management

### 背景
現在は作成日時のみ。締切管理ができないため実用度が限定的。

### 目的
- タスクの優先実行順を明確化
- 期限超過アラート / 近日期限の視認性向上

### スコープ
- `Todo` 型へ `dueDate?: Date` 追加
- 入力フォームに任意の期限入力 (日付ピッカー)
- 期限近接 (例: 24h以内) で黄色, 期限超過で赤表示
- ソートオプション: 作成日 / 期限 / 優先度

### 非スコープ
- 通知 (メール / Push)
- タイムゾーン複雑処理

### 提案
1. `TodoInput` 拡張 (日付 input[type=date])
2. 保存形式は ISO 文字列 (localStorage 用) → 復元で Date
3. リスト・カンバン両方でバッジ表示
4. ソート UI (セレクト) をフィルター付近へ追加

### タスクリスト
- [ ] 型拡張 & シリアライズ対応
- [ ] 入力 UI
- [ ] リスト表示 & スタイル
- [ ] ソートロジック + テスト
- [ ] 期限接近/超過ユニットテスト

### 受け入れ基準
- [ ] 期限設定なしでも動作
- [ ] 接近/超過の色分けが条件通り
- [ ] ソートが期待順を返すテスト Green

---

## :three: タグ付与 & タグフィルタリング機能
**優先度:** P1  
**カテゴリ:** feature / organization

### 背景
優先度のみでは分類が足りず、コンテキスト (例: "仕事", "個人", "学習") 別の整理が難しい。

### 目的
- タスクの分類と検索性向上
- フィルタ組み合わせ (状態 + タグ) で一覧性改善

### スコープ
- `Todo` に `tags?: string[]`
- 入力時: カンマ区切り or トークン入力 UI
- リスト上: タグバッジ表示 (最大3 + 省略 …)
- フィルタ: タグ一覧 (使用中タグ) から選択 (単一選択初期 / 将来複数対応)

### 非スコープ
- タグの色自動割当高度化 (初期は固定スタイル)
- タグ CRUD 管理画面

### 提案
1. 最低限: カンマ区切り文字列 → 配列トリム
2. フィルタ状態を `selectedTag` として保持 (localStorage 保存)
3. テスト: タグ追加 / フィルタ結果 / 永続化

### タスクリスト
- [ ] 型拡張
- [ ] 入力 UI (簡易トークン化)
- [ ] 表示バッジ
- [ ] フィルタ UI
- [ ] テスト (追加・フィルタ・永続化)

### 受け入れ基準
- [ ] タグ未入力でも既存動作変化なし
- [ ] タグ選択で対象タスクのみ表示
- [ ] リロード後も選択タグが保持

---

## :four: 統計ダッシュボード (完了率 / 週間推移 / 優先度分布)
**優先度:** P2  
**カテゴリ:** feature / analytics

### 背景
現在はカウンタ (アクティブ数/完了数) のみ。達成感や進捗傾向の把握が困難。

### 目的
- 習慣化 / モチベーション向上
- ボトルネック (停滞タスク) の可視化

### スコープ
- `/stats` ページ追加
- メトリクス例:
  - 全体完了率 (当日 / 累計)
  - 過去7日間の新規/完了推移 (簡易棒 or ラインチャート)
  - 優先度別件数
  - ステータス滞留 (in-progress > X 日)

### 非スコープ
- 外部 BI 連携
- 高度なグラフ (D3 カスタム)

### 提案
1. 軽量実装: 集計ロジックはクライアント計算
2. チャート: `chart.js` か `recharts` (依存最小評価)
3. 共通集計ユーティリティ `src/lib/stats.ts` 追加

### タスクリスト
- [ ] `/stats` ページ雛形
- [ ] 集計関数
- [ ] チャートコンポーネント
- [ ] 単体テスト (集計ロジック)

### 受け入れ基準
- [ ] 7日間推移が日付欠損なく表示
- [ ] 優先度分布が正しい数値
- [ ] タスク0件時の空表示メッセージ

---

## :five: データ同期 (将来拡張を見据えたバックアップ / インポート & エクスポート)
**優先度:** P2  
**カテゴリ:** feature / reliability / portability

### 背景
現状 localStorage のみ。ブラウザ変更・端末移行でデータ消失リスク。

### 目的
- 手動バックアップで安心感向上
- 将来クラウド同期/API 実装への段階的布石

### スコープ
- JSON エクスポート (ファイルダウンロード)
- JSON インポート (マージ or 置換選択)
- バージョン付きフォーマット `{ meta: { version: 1 }, todos: [...] }`

### 非スコープ
- リアルタイム多端末同期
- 認証 / ユーザ管理

### 提案
1. `repository.export()` / `repository.import()` (Issue "永続化層抽象化" と連携)
2. インポート時の検証 (不正項目スキップ + レポート)
3. UI: 設定セクション or フッター付近

### タスクリスト
- [ ] エクスポート関数
- [ ] インポート関数 (検証 & マージ戦略)
- [ ] UI ボタン (入力: file / output: download)
- [ ] テスト (往復 / 不正キー混入)

### 受け入れ基準
- [ ] エクスポート→インポートでデータ同一性保持
- [ ] 不正 JSON でアプリがクラッシュしない
- [ ] バージョンフィールドが欠落時警告表示

---

## ロードマップ提案
1. P0: :one:
2. P1: :two:, :three:
3. P2: :four:, :five:

---

## まとめ
これら機能により:
- 視覚化 (カンバン / 統計) で日常利用価値向上
- 組織化 (期限 / タグ) によるスケール耐性アップ
- データ喪失リスク低減 (バックアップ)
- 既存 `status` / `priority` / 永続化改善案との整合性を確保し拡張性を最大化

---

### 次アクション
- まず :one: を最小実装 (MVP: カード移動のみ) → 使用感フィードバック収集
- :two: / :three: を並行検討 (フォーム UI 拡張基盤を共通化)
- 余力で :five: の JSON I/O を先行実装し、テストデータ作成にも活用
