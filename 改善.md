<!--
このファイルは各セクションをそのまま GitHub Issue としてコピー & ペーストできる形で整理しています。
共通ラベル案: enhancement / tech-debt / a11y / testing / refactor / performance
-->

# 改善イシュー集 (コピー用テンプレ)

優先度レベル:
- P0: 緊急 (品質/信頼性/開発効率へ直接悪影響)
- P1: 近いスプリントで解消
- P2: 余力で改善 / 将来の負債予防

---

## :one: 空の `src/types/todo.ts` による型定義の混乱を解消する
**優先度:** P1  
**カテゴリ:** tech-debt

### 背景 / 現状
`Todo` 型は `src/types/shared/todo.ts` に存在する一方、`src/types/todo.ts` は空ファイル。エディタ検索や補完で誤参照を誘発。

### 課題 (Problem)
- 空ファイルが存在し目的と異なるファイルへ開いてしまう
- 今後別 contributor が二重定義を作成するリスク

### 提案 (Solution)
1. 空ファイルを削除 もしくは 下記 2 の形へ統一
2. 削除しない場合は明示的 re-export:
	```ts
	export * from './shared/todo';
	```
3. `CODING_GUIDELINES.md` に「型配置ポリシー」を追記

### タスクリスト
- [ ] `src/types/todo.ts` の扱い決定 (削除 or re-export)
- [ ] ガイドライン更新
- [ ] 既存参照の grep 確認

### 受け入れ基準 (Acceptance Criteria)
- [ ] プロジェクト内に空の型ファイルが存在しない
- [ ] `Todo` 型定義の所在がドキュメント化

---

## :two: 本番バンドルから不要な `console.*` ログを排除しロギング戦略を確立する
**優先度:** P0  
**カテゴリ:** performance / observability

### 背景 / 現状
`TodoApp` に多数のデバッグログ。ビルド時除去がなく本番でも実行される。

### 課題
- ユーザ環境で不要出力 (パフォーマンス & 情報ノイズ)
- ログレベル制御不能

### 提案
1. `src/lib/logger.ts` を追加 (簡易: `info`, `warn`, `error`, `debug`)
2. `NODE_ENV==='production'` の場合 `debug` は no-op
3. 既存 `console.log` を置換
4. 将来拡張時に外部送信 (SaaS など) できる抽象を確保

### タスクリスト
- [ ] ロガーユーティリティ作成
- [ ] 既存 `console.*` grep & 置換
- [ ] ESLint ルール `no-console` (error か warn) 設定 (logger 例外)

### 受け入れ基準
- [ ] 本番ビルド後 (analyze) に生の `console.log` 呼び出しが 0
- [ ] ログレベル毎の関数が存在し動作テスト済み

---

## :three: `TodoInput` の trim 仕様を統一し UX とテストの不一致を解消する
**優先度:** P0  
**カテゴリ:** spec / ux / testing

### 背景
現状: `TodoInput` は空判定だけ trim、値は未トリムで親へ渡す。親 (`addTodo`) で再度 `trim()` して保存。テストは「未トリムを渡す」仕様を固定。

### 課題
- 二重 trim ロジック (重複)
- テストが UX 改善 (余計な空白除去) を阻害する

### 提案
1. 入力送信時に `const normalized = inputValue.trim();` を渡す
2. テストを `expect(onAddTodo).toHaveBeenCalledWith('hello world')` へ修正
3. README に仕様 (前後空白は保存されない) を明記

### タスクリスト
- [ ] `TodoInput` 実装変更
- [ ] 対応テスト修正 & 追加 (空白だけ入力ケース)
- [ ] README 仕様セクション追記

### 受け入れ基準
- [ ] 余計な前後空白が保存されない
- [ ] 全テスト Green

---

## :four: localStorage 復元処理の型安全化と不正データ耐性強化
**優先度:** P1  
**カテゴリ:** data-integrity

### 背景
`any` でパース → `new Date(...)`。壊れた日付や欠損に対する防御なし。

### 提案
1. `SerializedTodo` 型定義 `{ id: string; text: string; completed: boolean; createdAt: string; ... }`
2. バリデーション: 軽量 (手書き) or Zod (導入コスト評価)
3. 復元ユーティリティ `parseStoredTodos(json: string): Todo[]` 追加
4. 不正要素はスキップ & ログ警告

### タスクリスト
- [ ] 型 & ユーティリティ追加
- [ ] エラーハンドリング実装
- [ ] 単体テスト (正常 / 壊れた JSON / 不正日付 / 欠損フィールド)

### 受け入れ基準
- [ ] 不正データ含む JSON でもクラッシュしない
- [ ] スキップ件数がテストで検証される

---

## :five: Fat Component 化した `TodoApp` を `useTodos` フックへ分離する
**優先度:** P1  
**カテゴリ:** refactor / maintainability

### 背景
ビジネスロジック (CRUD, filter, priority, status, persistence) が 1 コンポーネントに集中しテスト性低下。

### 提案
1. `src/hooks/useTodos.ts` を新設
2. 状態 + 操作関数 + 永続化副作用をフックへ移動
3. UI は表示とイベント配線のみへ簡素化

### タスクリスト
- [ ] フック作成 & 既存メソッド移植
- [ ] コンポーネントからロジック削除
- [ ] フック単体テスト (add/edit/delete, toggle, persistence)

### 受け入れ基準
- [ ] `TodoApp.tsx` のロジック行数 40% 以上削減
- [ ] ビジネスロジックがフック経由に統一

---

## :six: テスト実行時間最適化 (ts-jest からの改善 / CI 用プリセット)
**優先度:** P2  
**カテゴリ:** testing / performance

### 背景
ts-jest は型変換コストが比較的高い。Next.js 公式の `next/jest` + SWC 利用で速度改善余地。

### 提案
1. `next/jest` へ移行 (時間計測で効果が低い場合は現状維持)
2. `"test:ci": "jest --ci --coverage"` 追加
3. 型チェックを独立: `"typecheck": "tsc --noEmit"`

### タスクリスト
- [ ] ベースライン計測 (現行実行時間)
- [ ] 移行 PoC 分岐計測
- [ ] 最適案採用 & スクリプト追加

### 受け入れ基準
- [ ] 平均テスト時間 xx% 以上改善 (閾値記録)
- [ ] CI スクリプトで coverage 収集可能

---

## :seven: スクリプト/README/tsconfig の整合性を取り開発体験を改善
**優先度:** P2  
**カテゴリ:** dx / docs

### 課題
- README のディレクトリ名不一致
- `test:watch` スクリプト欠如
- `allowJs: true` が不要な可能性

### 提案
1. README のルートディレクトリ名を実際と一致させる
2. スクリプト追加:
	```json
	"test:watch": "jest --watch",
	"typecheck": "tsc --noEmit"
	```
3. `allowJs` の是非評価 (JS ファイルが無ければ false)

### タスクリスト
- [ ] README 修正
- [ ] `package.json` 更新
- [ ] JS ファイル存在確認 / 設定変更

### 受け入れ基準
- [ ] lint & test & typecheck が個別コマンドで走る
- [ ] README 指示通りクローン→起動が成功

---

## :eight: アクセシビリティ (a11y) の基礎整備
**優先度:** P2  
**カテゴリ:** a11y / quality

### 背景
フォームコントロールのラベル明示不足。カラーコントラスト未検証。

### 提案
1. `label + input` 構造化 (視覚非表示ラベル可)
2. ボタンに `aria-label` 追加 (必要なら)
3. `eslint-plugin-jsx-a11y` 導入
4. コントラストチェック (WCAG AA) を README の品質章に記載

### タスクリスト
- [ ] 依存追加 & ESLint 設定
- [ ] コンポーネント修正
- [ ] a11y テスト (role/name 存在) 追加

### 受け入れ基準
- [ ] a11y lint エラー 0
- [ ] 主要操作がスクリーンリーダーで識別可能

---

## :nine: 永続化層を抽象化し今後の同期/マイグレーションを容易にする
**優先度:** P2  
**カテゴリ:** architecture

### 背景
localStorage に直アクセス。拡張 (API 同期, IndexedDB, バージョン移行) が困難。

### 提案
1. `TodoRepository` インターフェース: `load()`, `save(todos)`, `clear()`
2. 実装: `LocalStorageTodoRepository`
3. バージョン番号 `meta.version` を保存 (将来マイグレーション用)

### タスクリスト
- [ ] インターフェース & 実装追加
- [ ] 既存コード差し替え
- [ ] リポジトリ単体テスト

### 受け入れ基準
- [ ] 直接 `localStorage.*` 呼び出しが Todo 関連コンポーネントから排除
- [ ] 将来別実装差し替え時の変更点が 1 ファイルに限定

---

## :keycap_ten: テストカバレッジとシナリオテストの拡充
**優先度:** P1  
**カテゴリ:** testing / quality

### 背景
ビジネスロジックの統合テストが不十分 (優先度更新 / ステータス遷移 / persistence round-trip)。

### 提案
1. 統合テスト `TodoApp`:
	- 優先度変更
	- ステータス遷移 todo → in-progress → done
	- 保存→再マウントで復元
	- クリア操作
	- trim 仕様 (Issue :three の決定内容)
2. Jest `coverageThreshold` 設定

### タスクリスト
- [ ] 追加テスト作成
- [ ] Jest 設定更新 (threshold)
- [ ] CI でバッジ (任意)

### 受け入れ基準
- [ ] Statements >= 85%, Branches >= 75% (目安)
- [ ] 主要ユースケースが回帰テスト化

---

## 実行順サマリ (ロードマップ参考)
1. P0: :two:, :three:
2. P1: :four:, :five:, :keycap_ten:
3. P2: :six:, :seven:, :eight:, :nine:
4. P1: :one: (随時)

---

## 参考: まとめ
これら改善により以下が期待できます:
- ログと仕様の明確化によるデバッグ効率向上
- データ耐久性と安全性向上
- コンポーネント肥大化抑制で保守性向上
- CI での品質ゲート (coverage / lint / a11y) 自動化
- 将来の機能拡張 (同期/外部 API) に対するアーキ強化

---

### 次アクション提案
- 直ちに Issue 化: :two:, :three:
- スプリント計画時に見積: :four:, :five:, :keycap_ten:
- 余力 or 中期改善枠: :six:, :seven:, :eight:, :nine:, :one:

---

必要ならこのファイルを分割し `/.github/ISSUE_TEMPLATE/` へテンプレ化してください。

